Download the following file: airflow.apache.org/docs/apache-airflow/2.5.1/docker-compose.yaml

Open Visual Studio Code create a new file .env and add the following :

AIRFLOW_IMAGE_NAME=apache/airflow:2.4.2
AIRFLOW_UID=50000

Add the following command in the terminal:

docker-compose up -d

Create Admin user using below command:

docker-compose run airflow-worker airflow users create --role Admin --username admin --email admin --firstname admin --lastname admin --password admin

Open in browser: http://localhost:8080/

To remove example DAGs:

in docker-compose.yaml change "AIRFLOW__CORE__LOAD_EXAMPLES: 'true'" to "AIRFLOW__CORE__LOAD_EXAMPLES: 'false'"

then in run terminal:

docker-compose up airflow-init

docker-compose up -d